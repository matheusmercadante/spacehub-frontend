apiVersion: v1
kind: Service
metadata:
  name: {{ include "spacehub-frontend.fullname" . }}
  labels:
    {{- include "spacehub-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spacehub-frontend.selectorLabels" . | nindent 4 }}
