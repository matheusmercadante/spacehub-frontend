apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spacehub-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "spacehub-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spacehub-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
